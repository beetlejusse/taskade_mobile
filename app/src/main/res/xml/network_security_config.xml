<?xml version="1.0" encoding="utf-8"?>
<!--
  Production traffic is TLS-only (wss:// / https://). Cleartext is permitted ONLY
  for local development hosts (the emulator loopback and localhost) so a backend
  running on the dev machine is reachable from the debug build. See PRD §10.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <!-- This PC's LAN IP, for testing on a physical device over Wi-Fi.
             Update if the PC's address changes. -->
        <domain includeSubdomains="true">192.168.1.4</domain>
    </domain-config>
</network-security-config>
